.DS_Store

# ── Dados pessoais reais — NUNCA neste repositório (ele é PÚBLICO) ──────────
# A base de nomes, exports e backups moram no Google Drive:
#   Meu Drive/Pipe X/CRM Life Planner/
# Ver LEIA-ME.md dessa pasta. Estas regras são a rede de segurança caso um
# arquivo desses caia aqui por engano.
base-de-nomes/
contatos.json
*_contato_origens.csv
*_dup_fila.csv
BASE_TRATADA*.xlsx
data_*.csv

# Benchmark do concorrente (Global CRM) — material de terceiro garimpado de grupo privado; NUNCA publicar (repo é público). Home certa = acervo no Drive.
docs/benchmark/
